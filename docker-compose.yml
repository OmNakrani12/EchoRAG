services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: voicerag_qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: voicerag_backend
    image: echorag:latest
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_COLLECTION=${QDRANT_COLLECTION:-voicerag_audio}
      - EMBEDDING_DIMENSION=${EMBEDDING_DIMENSION:-768}
      - TOP_K=${TOP_K:-5}
      - SIMILARITY_THRESHOLD=${SIMILARITY_THRESHOLD:-0.35}
      - STORAGE_PATH=/app/storage
      - DATABASE_URL=${DATABASE_URL:-sqlite:////app/storage/voicerag.db}
    volumes:
      - ./backend/storage:/app/storage
    depends_on:
      - qdrant
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: voicerag_frontend
    image: echorag-frontend:latest
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  qdrant_storage:

